import { Routes } from '@angular/router';
import { AppSettingsPageComponent } from './components/app-settings-page/app-settings.page.component';

export const routes: Routes = [
  { path: '', component: AppSettingsPageComponent },
];
